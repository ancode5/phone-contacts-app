1. Stop npm run dev with Ctrl+C.
2. Extract this archive directly into the project root next to package.json.
3. Confirm replacement of existing files.
4. Do NOT run old installers. Run only:
   npm run typecheck
   npm run build
   npm run dev
5. Open http://localhost:5173 and press Ctrl+Shift+R once.
6. Sign in with Yandex. The GitHub storage form must appear.
7. Commit only after the local test succeeds:
   git add .gitignore src/App.tsx src/main.tsx src/components/GitHubStorageGate.tsx src/services/cloudSync.ts src/services/githubStorage.ts
   git commit -m "Fix GitHub encrypted storage integration"
   git push origin feature/cloud-sync
