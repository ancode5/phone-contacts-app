1. Extract this archive directly into the project root: D:\python\phone-contacts-app-fixed
2. Run INSTALL-FIX.cmd
3. Run npm run dev
4. Open http://localhost:5173
5. Sign in with Yandex. After return, the GitHub storage connection form must appear.

The installer is ASCII-only to avoid Windows CMD encoding problems.
